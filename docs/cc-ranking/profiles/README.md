Put opt-in vertical character profile images here.

Register each image in `web/profile-registry.js` by `character_id` when available, or by
`캐릭터명@서버` for manual entries. When running the `web` directory through Vite, use
root-relative URLs such as `/static/profiles/example.webp`.
